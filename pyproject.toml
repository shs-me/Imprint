[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "imprint"
version = "0.1.0"
description = "Multiprocess order-flow trading engine for crypto futures backtesting and live execution"
readme = "README.md"
requires-python = ">=3.13,<3.14"
authors = [
    { name = "Sherali Safaralizoda", email = "imprint+shs06main@gmail.com" }
]
keywords = [
    "trading-engine",
    "order-flow",
    "footprint-chart",
    "backtesting",
    "crypto-trading",
    "algorithmic-trading",
    "market-microstructure",
    "quantitative-finance",
    "zero-copy-ipc",
    "shared-memory",
]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Developers",
    "Intended Audience :: Financial and Insurance Industry",
    "Topic :: Office/Business :: Financial :: Investment",
    "Programming Language :: Python :: 3.13",
    "Operating System :: POSIX :: Linux",
    "Operating System :: Microsoft :: Windows",
    "License :: OSI Approved :: Mozilla Public License 2.0 (MPL 2.0)",
]

dependencies = [
    "numpy==2.4.6",
    "numba==0.66.0",
    "msgspec==0.21.1",
    "websockets==16.1.1",
    "httpx==0.28.1",
    "loguru==0.7.3",
]

[project.optional-dependencies]
dev = [
    "pytest==9.1.1",
    "pytest-cov==7.1.0",
    "ruff==0.16.6",
]

plot = [
    "plotly==7.0.0",
]

all = [
    "imprint[dev,plot]",
]

[project.urls]
Homepage = "https://github.com/shs-me/Imprint"
Repository = "https://github.com/shs-me/Imprint.git"

[tool.hatch.build.targets.wheel]
packages = ["src/imprint"]

[tool.pytest.ini_options]
testpaths = [
    "tests/unit",
    "tests/integration"
]
pythonpath = ["src"]
addopts = "--cov=imprint --cov-report=term-missing"

[tool.ruff]
line-length = 80

[tool.ruff.lint]
ignore = [
     "BLE001"
]

[tool.pyright]
pythonVersion = "3.13"
pythonPlatform = "All"
venvPath = "."
venv = ".venv"

reportAny = false
reportExplicitAny = false
reportPrivateUsage = false
reportUnusedCallResult = false
